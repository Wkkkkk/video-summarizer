# Required for the default summarizer (gemini-2.5-pro) and for --visual (gemini-pro).
GEMINI_API_KEY=your-key-here

# Required only for the Claude summary backend (--summary-backend claude) and as
# the automatic fallback when the Gemini summary errors/rate-limits.
ANTHROPIC_API_KEY=your-key-here
